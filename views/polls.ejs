<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Created Polls</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">
    <div class="container mx-auto px-4">
        <div class="flex justify-center">
            <div class="w-full max-w-md my-10">
                <div class="bg-white shadow-md rounded-lg p-6">
                    <h5 class="text-xl font-semibold mb-4">Your Polls</h5>
                    <ul class="space-y-3">
                        <% for (let p of polls) { %>
                            <li class="p-4 bg-gray-50 rounded-lg shadow hover:bg-gray-100">
                                <a href="/polls/<%= p._id %>" class="text-blue-500 hover:underline">
                                    <%= p.title %>
                                </a>
                            </li>
                            <% } %>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</body>

</html>